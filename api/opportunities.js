function clamp(n,min,max){return Math.max(min,Math.min(max,n))}
function fmtDate(d){return `${String(d.getMonth()+1).padStart(2,'0')}/${String(d.getDate()).padStart(2,'0')}/${d.getFullYear()}`}
let memoryCache={items:null,updatedAt:0};
const MEMORY_TTL=60*60*1000;
function scoreOpportunity(o){let score=50;const positives=[],risks=[];const text=`${o.title||''} ${o.type||''} ${o.tags?.join(' ')||''}`.toLowerCase();const goods=['printer','computer','laptop','monitor','chair','furniture','office supplies','paper','signage','uniform','equipment','tool','parts','supplies','appliance','battery','toner','cartridge','food','catering'];const simpleServices=['cleaning','janitorial','landscaping','grass cutting','snow removal','moving','photography','translation','printing','graphic design','website','pest control','grounds maintenance'];const hard=['construction','engineering','architect','asbestos','hazmat','medical','surgical','security clearance','aircraft','weapon','ammunition','nuclear','demolition','roof replacement','electrical installation','plumbing installation'];const research=['sources sought','request for information','special notice','presolicitation','intent to sole source','industry day'];if(goods.some(k=>text.includes(k))){score+=16;positives.push('Commercial product sourcing potential')}if(simpleServices.some(k=>text.includes(k))){score+=12;positives.push('Relatively straightforward service category')}if(hard.some(k=>text.includes(k))){score-=22;risks.push('Specialized or capital-intensive work')}if(research.some(k=>text.includes(k))){score-=28;risks.push('May be market research rather than an open bid')}if(/award|justification/.test(String(o.type||'').toLowerCase())){score-=40;risks.push('Appears to be an award/post-award notice')}if(o.setAside&&/small business/i.test(o.setAside)){score+=8;positives.push('Small-business set-aside')}const country=(o.country||'').toUpperCase();if(country&&['USA','US','UNITED STATES','UNITED STATES OF AMERICA'].includes(country)){score+=7;positives.push('U.S. place of performance')}else if(country){score-=24;risks.push(`Foreign place of performance: ${o.country}`)}const days=o.deadline?Math.ceil((new Date(o.deadline)-new Date())/86400000):null;if(days!==null){if(days>=10&&days<=45){score+=10;positives.push(`${days} days to respond`)}else if(days>=5){score+=4}else if(days>=0){score-=14;risks.push('Very short response window')}else{score-=45;risks.push('Response deadline has passed')}}else{score-=4;risks.push('No response deadline shown')}score=clamp(score,0,100);const verdict=score>=85?'Exceptional':score>=70?'Strong':score>=55?'Investigate':'Skip';const recommendation=score>=85?'High-priority review':score>=70?'Worth reviewing now':score>=55?'Review if it matches your capabilities':'Low-priority / likely skip';return{score,verdict,recommendation,positives:positives.slice(0,3),risks:risks.slice(0,3)}}
function mapSam(x){const pop=x.placeOfPerformance||{};const country=pop.country?.name||pop.country?.code||'';const city=pop.city?.name||pop.city||'';const state=pop.state?.code||pop.state||'';const location=[city,state,country&&!['USA','US','United States','United States of America'].includes(country)?country:null].filter(Boolean).join(', ')||country||'See notice';const setAside=x.typeOfSetAsideDescription||x.setAside||x.typeOfSetAside||x.setAsideCode||'Not specified';const amount=x.award?.amount||x.data?.award?.amount||null;return{id:x.noticeId,source:'SAM.gov',title:x.title||'Untitled opportunity',agency:x.fullParentPathName||[x.department,x.subTier,x.office].filter(Boolean).join(' · ')||'Federal agency',postedDate:x.postedDate,deadline:x.responseDeadLine||x.reponseDeadLine||null,location,country,type:x.type||'Opportunity',setAside,description:'BidScout screened this listing from SAM.gov metadata. Open the original solicitation for complete requirements and attachments.',url:`https://sam.gov/opp/${encodeURIComponent(x.noticeId)}/view`,value:amount?`$${Number(amount).toLocaleString('en-US')}`:'Not disclosed',tags:[x.naicsCode?`NAICS ${x.naicsCode}`:null,x.classificationCode?`PSC ${x.classificationCode}`:null].filter(Boolean),active:String(x.active||'Yes').toLowerCase()==='yes'}}
async function fetchSam(){const apiKey=process.env.SAM_API_KEY;if(!apiKey)return{items:[],error:'Government feed configuration unavailable',code:'NO_KEY'};const now=Date.now();if(memoryCache.items&&now-memoryCache.updatedAt<MEMORY_TTL)return{items:memoryCache.items,fromCache:true};const today=new Date(),from=new Date(today);from.setDate(from.getDate()-7);const p=new URLSearchParams({api_key:apiKey,limit:'100',offset:'0',postedFrom:fmtDate(from),postedTo:fmtDate(today)});const r=await fetch(`https://api.sam.gov/opportunities/v2/search?${p}`,{headers:{accept:'application/json'}});if(!r.ok){const code=r.status===429?'RATE_LIMITED':`HTTP_${r.status}`;if(memoryCache.items)return{items:memoryCache.items,fromCache:true,stale:true,error:'Live refresh temporarily unavailable',code};return{items:[],error:'Government source temporarily unavailable',code}}const j=await r.json();const items=(j.opportunitiesData||[]).map(mapSam).filter(o=>o.active!==false).map(o=>({...o,...scoreOpportunity(o)})).sort((a,b)=>b.score-a.score);if(items.length){memoryCache={items,updatedAt:Date.now()}}return{items}}
module.exports=async function handler(req,res){const result=await fetchSam();const hasLive=result.items&&result.items.length>0;if(hasLive){res.setHeader('Cache-Control','public, max-age=60');res.setHeader('Vercel-CDN-Cache-Control','public, max-age=3600, stale-while-revalidate=21600')}else{res.setHeader('Cache-Control','no-store')}const mode=hasLive?(result.stale?'stale':'live'):(result.code==='RATE_LIMITED'?'rate_limited':'unavailable');res.status(200).json({mode,error:result.error||null,code:result.code||null,updatedAt:new Date().toISOString(),items:hasLive?result.items:[]})}
